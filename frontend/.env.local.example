# Frontend → backend wiring. Copy to .env.local.
# The Next.js API routes (/api/diagnose, /api/outcome) call FastAPI server-side.
# Defaults to http://localhost:8000 if unset, so local dev works out of the box.
BACKEND_URL=http://localhost:8000
